{
  "name": "roblox-webhook",
  "version": "1.0.0",
  "description": "Secure one-time-use webhook proxy with key authentication",
  "main": "api/webhook.js",
  "scripts": {
    "start": "node api/webhook.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "crypto": "^1.0.1",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "@vercel/node": "^4.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
